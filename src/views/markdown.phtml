<?php

if (!empty($pageMeta->Title)) {
    echo "<h1>$pageMeta->Title</h1>";
}
if (!empty($pageMeta->Date)) {
    echo "<p>" . date("jS F Y", strtotime($pageMeta->Date)) . "</p>";
}
print($markdown);
if ($sharelink) {
    print('<hr /><a href="#" target="mastodon" class="mastodon-share">Share to Mastodon</a>');
}
